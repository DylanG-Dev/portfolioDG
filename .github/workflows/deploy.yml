name: Deploy Project

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    container:
      image: node:24.12.0
    steps:
      - uses: action/checkout@v3
      with:
        node-version: 24.12.0
      - run: npm ci
      - run: npm run build